import { createAction } from './xs-state.jsx'

export const playerNameChange = createAction()
export const playerFactionChange = createAction()
export const createPlayerAccount = createAction()
export const foundColony = createAction()
export const useGenesis = createAction()
